# Node's own artifacts. The build *output* is not here - it lands in ../ui and
# is committed on purpose, so a reviewer needs Python and no toolchain.
node_modules/
.vite/
node_modules/.vite/
*.tsbuildinfo
vite.config.ts.timestamp-*
npm-debug.log*
